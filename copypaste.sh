#!/bin/bash
chmod +x temp.sh
chmod +x blueplease
sudo cp blueplease /usr/bin
chmod +x extract
sudo cp extract /usr/bin
chmod +x lazywisdom
sudo cp lazywisdom /usr/bin
chmod +x vsop
sudo cp vsop /usr/bin
chmod +x xx
sudo cp xx /usr/bin

